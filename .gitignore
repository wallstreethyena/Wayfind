node_modules
.next
.env.local
.DS_Store
test-results
playwright-report

# candidate-set seeder crash-recovery checkpoint (local, not committed)
.seed-progress.*.json
.vercel
.env*

# scripts/atlas-batch.mjs — submitted-batch state (custom_id -> row map, so a
# crash between submit and poll can --resume without resubmitting). Local, not committed.
.atlas-batch-state*.json

# local inventory dump (not committed)
wf-inventory-list.txt

# index→library promoter: audit + rollback backups (local, not committed)
.promote-audit/
.promote-backup/

# index→library promoter: cached raw enrichment (local, not committed)
.promote-cache/

# Claude Code: share agent definitions, keep per-machine settings local
.claude/*
!.claude/agents/
.claude/settings.local.json

# local agent scratch — not shared
.worktrees/
_to_delete/
_incoming/
tmp/
.cowork-parked/
.claude-backup/

# Transient JSX compile output from scripts/lib/jsxLoad.mjs (guard-only, self-cleaning)
.wf-jsx-*/

# Exploding Topics — licensed CSV exports must never be committed.
# The synthetic fixtures in scripts/fixtures/trends/ ARE committed on purpose
# (they are invented data this repo wrote); the negation below keeps them.
/private/trends/
/data/trends/
*exploding-topics*.csv
*exploding_topics*.csv
trend-export*.csv
!scripts/fixtures/trends/*.csv
.rail-shots/

# Synthetic-monitor failure evidence (scripts/run-synthetic-monitor.mjs) —
# timestamped per run, regenerated by every invocation, never committed.
/synthetic-evidence/
